import '@testing-library/jest-dom';
import { fireEvent, render, screen } from '@testing-library/react'
import App from '../App';

describe('App test', () => {
  it('can run tests', () => {
    // did this to show the axios failure 
    // and the fix added to test script package.json
  })
})
